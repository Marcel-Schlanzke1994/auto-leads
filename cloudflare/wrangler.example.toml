# Example config only. Keep real values in local wrangler.toml (gitignored).
# Never commit production IDs, API tokens, or domains.
name = "auto-leads-worker"
main = "src/index.ts"
compatibility_date = "2026-04-28"
workers_dev = true

[vars]
VERSION = "0.1.0"

[[durable_objects.bindings]]
name = "OUTREACH_RATE_LIMITER"
class_name = "OutreachRateLimiter"

# Example-only migration for local/prototype use.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["OutreachRateLimiter"]
